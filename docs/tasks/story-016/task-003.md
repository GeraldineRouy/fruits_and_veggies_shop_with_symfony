# Tâche #003 - Story #016 : Tests automatisés de la correction

## Objectif

Ajouter des tests unitaires et d'intégration qui valident la correction du bug de paiement : les IDs des OrderLine sont auto-générés sans conflit, la migration de séquence fonctionne, et les erreurs en paiement sont gérées proprement sans exception 500.

## Contexte

- Story #016 : `docs/stories/story-016.md`
- Dépend de : Tâche #001 (migration séquence), Tâche #002 (gestion d'erreurs)
- Tests existants : `tests/Integration/Controller/CheckoutControllerTest.php` (déjà créé par Story #013), `tests/Unit/Service/OrderServiceTest.php`
- Framework de test : PHPUnit 13 (attribut `#[Test]`)

## Prompt

En tant qu'agent de code, tu dois implémenter ce qui suit.

### Description fonctionnelle

Les tests doivent valider les critères d'acceptation de la Story #016, en plus des tests existants déjà présents.

**Ce qui est déjà testé (Story #013) :**
- Accès à la page paiement sans authentification → redirection login
- Accès à la page paiement avec panier vide → redirection panier
- Page paiement affiche les champs pré-remplis
- Soumission du paiement crée la commande et redirige vers confirmation
- Accès confirmation sans auth → redirection login
- Page confirmation affiche les détails
- Accès commande autre utilisateur → 403
- Commande inexistante → 404
- Lien "Retour à l'accueil" présent
- Soumission avec panier vide → redirection panier

**Ce qu'il faut AJOUTER :**
- Vérifier que les IDs des OrderLine sont bien auto-générés et uniques après création
- Vérifier que soumissions multiples créent des commandes sans conflit de clé primaire
- Vérifier que le message d'erreur explicite s'affiche en cas d'exception inattendue (via simulation)

### Fichiers concernés

| Fichier | Action | Description |
|---------|--------|-------------|
| `tests/Unit/Service/OrderServiceTest.php` | Modifier | Ajouter test unitaire : auto-génération des IDs OrderLine |
| `tests/Integration/Controller/CheckoutControllerTest.php` | Modifier | Ajouter tests d'intégration pour la correction du bug |
| `tests/E2E/checkout.spec.js` | Vérifier | Déjà existant — doit passer sans erreur |

### Signatures

```php
// Ajouter dans OrderServiceTest.php

#[Test]
public function orderLinesHaveAutoGeneratedIds(): void
{
    // Vérifier que createFromCart() ne définit pas manuellement les IDs
    // et que les OrderLine sont persistées avec id = null (auto-généré par Doctrine)
}

#[Test]
public function createFromCartPropagatesUnexpectedExceptions(): void
{
    // Vérifier que les exceptions Doctrine/Runtime sont propagées depuis createFromCart
}
```

```php
// Ajouter dans CheckoutControllerTest.php

#[Test]
public function multiplePaymentsCreateOrdersWithoutUniqueViolation(): void
{
    // Effectuer 3 paiements consécutifs (créer/utilisateur)
    // Vérifier qu'aucun n'échoue avec UniqueConstraintViolationException
}

#[Test]
public function orderLineIdsAreAutoGeneratedAndUnique(): void
{
    // Après paiement, vérifier que tous les OrderLine ont des IDs non nuls et uniques
}
```

### Contraintes techniques

- **Framework** : PHPUnit 13 avec attribut `#[Test]`
- **Bootstrap** : `tests/bootstrap.php` charge `.env` — les tests d'intégration utilisent la base de test automatiquement
- **Client authentifié** : Utiliser `$client->loginUser($user)` de Symfony
- **Nettoyage** : Vider les tables dans `setUp()` comme déjà fait dans le fichier existant
- **Assertions** : Utiliser les méthodes d'assertion Symfony (`assertResponseStatusCodeSame`, `assertSelectorTextContains`, etc.)

### Tests à implémenter

#### Tests unitaires — `tests/Unit/Service/OrderServiceTest.php`

**Scénario 1 : Les OrderLine sont persistées avec un ID auto-généré (null avant flush)**

Vérifier que dans `createFromCart()`, les OrderLine sont créées sans ID défini manuellement :

```php
#[Test]
public function orderLinesHaveAutoGeneratedIds(): void
{
    $user = new User();
    $cart = $this->createCartWithItems($user);

    $cartRepository = $this->createMock(CartRepository::class);
    $cartRepository->expects($this->once())
        ->method('findOneBy')
        ->with(['user' => $user])
        ->willReturn($cart);

    $entityManager = $this->createMock(EntityManagerInterface::class);

    $persistedEntities = [];
    $entityManager->expects($this->exactly(3))->method('persist')
        ->willReturnCallback(function ($entity) use (&$persistedEntities) {
            $persistedEntities[] = $entity;
        });

    $entityManager->method('flush');

    $messageBus = $this->createMock(MessageBusInterface::class);
    $messageBus->method('dispatch')->willReturn(new Envelope(new \stdClass()));

    $service = $this->createService(
        entityManager: $entityManager,
        cartRepository: $cartRepository,
        messageBus: $messageBus,
    );

    $service->createFromCart($user);

    // Vérifier que les OrderLine persistées ont un ID null (auto-généré par Doctrine)
    $orderLines = array_filter($persistedEntities, fn($e) => $e instanceof OrderLine);
    $this->assertCount(2, $orderLines);
    foreach ($orderLines as $orderLine) {
        $this->assertNull($orderLine->getId(), 'OrderLine ID must be null before flush (auto-generated)');
    }
}
```

**Scénario 2 : Les exceptions inattendues sont propagées par createFromCart**

```php
#[Test]
public function createFromCartPropagatesUnexpectedExceptions(): void
{
    $user = new User();
    $cart = $this->createCartWithItems($user);

    $cartRepository = $this->createMock(CartRepository::class);
    $cartRepository->expects($this->once())
        ->method('findOneBy')
        ->with(['user' => $user])
        ->willReturn($cart);

    $entityManager = $this->createMock(EntityManagerInterface::class);
    $entityManager->expects($this->once())
        ->method('flush')
        ->willThrowException(new \RuntimeException('Erreur Doctrine simulée'));

    $service = $this->createService(
        entityManager: $entityManager,
        cartRepository: $cartRepository,
    );

    $this->expectException(\RuntimeException::class);
    $this->expectExceptionMessage('Erreur Doctrine simulée');

    $service->createFromCart($user);
}
```

#### Tests d'intégration — `tests/Integration/Controller/CheckoutControllerTest.php`

Ajouter ces tests dans le fichier EXISTANT.

**Scénario 3 : Paiements multiples sans conflit de clé primaire**

```php
#[Test]
public function multiplePaymentsCreateOrdersWithoutUniqueViolation(): void
{
    // Créer 2 utilisateurs distincts
    $user1 = $this->createUser('buyer_multi_1@test.com');
    $user1category = $this->createCategory('Fruits');
    $user1product1 = $this->createProduct('Pomme', '2.50', $user1category);
    $user1product2 = $this->createProduct('Banane', '1.80', $user1category);
    $this->createCartWithItems($user1, [
        [$user1product1, 2, '2.50'],
        [$user1product2, 1, '1.80'],
    ]);

    $user2 = $this->createUser('buyer_multi_2@test.com');
    $user2category = $this->createCategory('Légumes');
    $user2product1 = $this->createProduct('Carotte', '1.50', $user2category);
    $user2product2 = $this->createProduct('Tomate', '2.00', $user2category);
    $user2product3 = $this->createProduct('Salade', '1.20', $user2category);
    $this->createCartWithItems($user2, [
        [$user2product1, 3, '1.50'],
        [$user2product2, 2, '2.00'],
        [$user2product3, 1, '1.20'],
    ]);

    // Premier paiement — utilisateur 1
    $this->client->loginUser($user1);
    $this->client->request('POST', '/commande/paiement');
    $this->assertResponseRedirects();
    $this->client->followRedirect();
    $this->assertStringContainsString('Merci pour votre commande', $this->client->getResponse()->getContent());

    // Deuxième paiement — utilisateur 2
    $this->client->loginUser($user2);
    $this->client->request('POST', '/commande/paiement');
    $this->assertResponseRedirects();
    $this->client->followRedirect();
    $this->assertStringContainsString('Merci pour votre commande', $this->client->getResponse()->getContent());
}
```

**Scénario 4 : Les IDs des OrderLine sont auto-générés et uniques**

```php
#[Test]
public function orderLineIdsAreAutoGeneratedAndUnique(): void
{
    $user = $this->createUser('buyer_ids@test.com');
    $category = $this->createCategory('Fruits');
    $product1 = $this->createProduct('Fraise', '4.00', $category);
    $product2 = $this->createProduct('Myrtille', '5.00', $category);
    $product3 = $this->createProduct('Framboise', '6.00', $category);
    $this->createCartWithItems($user, [
        [$product1, 2, '4.00'],
        [$product2, 1, '5.00'],
        [$product3, 3, '6.00'],
    ]);

    $this->client->loginUser($user);
    $this->client->request('POST', '/commande/paiement');

    $this->assertResponseRedirects();

    // Vérifier les IDs des OrderLine en base
    $this->entityManager->clear();
    $order = $this->entityManager->getRepository(Order::class)->findOneBy(['user' => $user]);
    $this->assertNotNull($order);

    $lineIds = [];
    foreach ($order->getOrderLines() as $line) {
        $this->assertNotNull($line->getId(), 'OrderLine ID must not be null after flush');
        $lineIds[] = $line->getId();
    }

    $this->assertCount(3, $lineIds);
    $this->assertCount(3, array_unique($lineIds), 'OrderLine IDs must be unique');
}
```

#### Test E2E — `tests/E2E/checkout.spec.js`

Ce test existe déjà et couvre le parcours complet. Vérifier qu'il passe après la correction. Aucune modification nécessaire.

### Documentation

Mettre à jour `docs/features/order-process.md` pour mentionner que le bug de conflit d'ID a été corrigé et qu'un message d'erreur explicite s'affiche désormais en cas de problème lors du paiement.

### Exemples d'utilisation

```bash
# Exécuter tous les tests
bin/phpunit

# Exécuter les tests de la correction
bin/phpunit tests/Integration/Controller/CheckoutControllerTest.php
bin/phpunit tests/Unit/Service/OrderServiceTest.php --filter="orderLinesHaveAutoGeneratedIds|createFromCartPropagatesUnexpectedExceptions"

# Exécuter le test E2E
npx playwright test tests/E2E/checkout.spec.js
```
