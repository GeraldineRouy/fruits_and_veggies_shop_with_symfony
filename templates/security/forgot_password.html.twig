{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
<div class="forgot-password-container max-w-md mx-auto mt-10">
    <div class="bg-white rounded-xl shadow-md p-8">
        <h1 class="text-2xl font-bold text-gray-800 text-center mb-6">Mot de passe oublié</h1>

        <p class="text-gray-600 text-sm mb-6">Saisissez votre email pour recevoir un lien de réinitialisation.</p>

        {{ form_start(form, {attr: {class: 'space-y-4'}}) }}
            {{ form_row(form.email) }}

            <button type="submit" class="btn btn-primary w-full bg-brand-600 text-white px-4 py-2 rounded-lg font-medium hover:bg-brand-700 transition-colors">Envoyer</button>
        {{ form_end(form) }}

        <p class="login-links mt-4 text-center text-sm text-gray-600">
            <a href="{{ path('app_login') }}" class="text-brand-600 hover:text-brand-800">Retour à la connexion</a>
        </p>
    </div>
</div>
{% endblock %}
